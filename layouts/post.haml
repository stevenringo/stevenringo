-render "base" do
  .post
    .heading
      %span.date
        #{@item.created_at.strftime("%d %B")}
        %span.year
          #{@item.created_at.strftime("%Y")}
        
      %h1
        #{@item[:title]}
    .entry
      ~ yield
    #disqus_thread
      %script{:src => "http://disqus.com/forums/stevenringo/embed.js", :type => "text/javascript"}
        console.log(disqus_url)
      %noscript
        %a{:href => "http://disqus.com/forums/stevenringo/?url=ref"} View the discussion thread.
